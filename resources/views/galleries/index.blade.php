@extends('layouts.dashboard')
@section('title', 'Galeri Karya')
@section('content')
<div class="bg-white dark:bg-slate-900 rounded-2xl p-6 border border-slate-200 dark:border-slate-800"><div class="flex justify-between mb-4"><form method="GET"><input type="text" name="q" value="{{ request('q') }}" placeholder="Cari galeri" class="rounded-xl border-slate-300 dark:bg-slate-800"></form><a href="{{ route('galleries.create') }}" class="px-4 py-2 bg-red-600 text-white rounded-xl">Tambah</a></div><table class="w-full"><thead><tr class="border-b border-slate-200 dark:border-slate-800"><th class="py-2">Judul</th><th>Kategori</th><th>Tanggal</th><th class="text-right">Aksi</th></tr></thead><tbody>@forelse($galleries as $gallery)<tr class="border-b border-slate-100 dark:border-slate-800"><td class="py-2">{{ $gallery->title }}</td><td>{{ $gallery->category }}</td><td>{{ optional($gallery->event_date)->format('d M Y') }}</td><td class="text-right"><a href="{{ route('galleries.show',$gallery) }}" class="text-blue-600">Detail</a> <a href="{{ route('galleries.edit',$gallery) }}" class="text-amber-600">Edit</a> <form action="{{ route('galleries.destroy',$gallery) }}" method="POST" class="inline">@csrf @method('DELETE')<button class="text-red-600" onclick="return confirm('Hapus data?')">Hapus</button></form></td></tr>@empty<tr><td colspan="4" class="py-3 text-slate-500">Tidak ada data.</td></tr>@endforelse</tbody></table><div class="mt-4">{{ $galleries->links() }}</div></div>
@endsection
