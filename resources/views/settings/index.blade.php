@extends('layouts.dashboard')
@section('title', 'Pengaturan')
@section('content')
<div class="bg-white dark:bg-slate-900 rounded-2xl p-6 border border-slate-200 dark:border-slate-800"><div class="flex justify-between mb-4"><form method="GET"><input type="text" name="q" value="{{ request('q') }}" placeholder="Cari key" class="rounded-xl border-slate-300 dark:bg-slate-800"></form><a href="{{ route('settings.create') }}" class="px-4 py-2 bg-red-600 text-white rounded-xl">Tambah</a></div><table class="w-full"><thead><tr class="border-b border-slate-200 dark:border-slate-800"><th class="py-2">Key</th><th>Group</th><th>Type</th><th class="text-right">Aksi</th></tr></thead><tbody>@forelse($settings as $setting)<tr class="border-b border-slate-100 dark:border-slate-800"><td class="py-2">{{ $setting->key }}</td><td>{{ $setting->group }}</td><td>{{ $setting->type }}</td><td class="text-right"><a href="{{ route('settings.show',$setting) }}" class="text-blue-600">Detail</a> <a href="{{ route('settings.edit',$setting) }}" class="text-amber-600">Edit</a> <form action="{{ route('settings.destroy',$setting) }}" method="POST" class="inline">@csrf @method('DELETE')<button class="text-red-600" onclick="return confirm('Hapus data?')">Hapus</button></form></td></tr>@empty<tr><td colspan="4" class="py-3 text-slate-500">Tidak ada data.</td></tr>@endforelse</tbody></table><div class="mt-4">{{ $settings->links() }}</div></div>
@endsection
