@extends('layouts.dashboard')
@section('title', 'Manajemen Tag')
@section('content')
<div class="bg-white dark:bg-slate-900 rounded-2xl p-6 border border-slate-200 dark:border-slate-800"><div class="flex justify-between mb-4"><form method="GET"><input type="text" name="q" value="{{ request('q') }}" placeholder="Cari tag" class="rounded-xl border-slate-300 dark:bg-slate-800"></form><a href="{{ route('tags.create') }}" class="px-4 py-2 bg-red-600 text-white rounded-xl">Tambah</a></div><table class="w-full"><thead><tr class="border-b border-slate-200 dark:border-slate-800"><th class="py-2">Nama</th><th>Slug</th><th class="text-right">Aksi</th></tr></thead><tbody>@forelse($tags as $tag)<tr class="border-b border-slate-100 dark:border-slate-800"><td class="py-2">{{ $tag->name }}</td><td>{{ $tag->slug }}</td><td class="text-right"><a href="{{ route('tags.show',$tag) }}" class="text-blue-600">Detail</a> <a href="{{ route('tags.edit',$tag) }}" class="text-amber-600">Edit</a> <form action="{{ route('tags.destroy',$tag) }}" method="POST" class="inline">@csrf @method('DELETE')<button class="text-red-600" onclick="return confirm('Hapus data?')">Hapus</button></form></td></tr>@empty<tr><td colspan="3" class="py-3 text-slate-500">Tidak ada data.</td></tr>@endforelse</tbody></table><div class="mt-4">{{ $tags->links() }}</div></div>
@endsection
